DATABASE_URL=postgresql://motorhome:motorhome@127.0.0.1:5433/motorhome
AUTH_SESSION_SECRET=replace-with-a-random-secret-of-at-least-32-characters
# Railway/staging: generate with `npm run pin:hash -- <pin>`.
AUTH_PIN_HASH=pbkdf2_sha256$iterations$salt$hash
# Local-only alternative for first boot:
# AUTH_BOOTSTRAP_PIN=246810
